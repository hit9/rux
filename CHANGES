v0.6.5
------

- beta version released

v0.6.3
------

- use `clr` instead of `default` as default theme

v0.6.2
------

- add option `root` in config.toml

v0.6.1
------

- minor typo fixes

v0.5.9
------

- Remove command `rux restart`
- Add optional `<port>` to command: `rux start`, `rux serve`

v0.5.8
------

- PDF command campatible with OSX 

v0.5.7
------

- Docs & README fixing

v0.5.6
------

- Add ability to generate PDF

v0.5.5
------

- use **SINGLE** process to build blog.

v0.5.3
------

- use C shared library to parse out title, title picture and body

v0.5.1
-------

- Update theme submodule
- some typo fixed

v0.5.0(**NOT Backward Compatible**)
-----------------------------------

- New post syntax
- Add title_pic to post model
- New theme rux-default


v0.4.0
------

- rewrite `generator.py`, use mutiple processes to build
  site.

- some typo fix

- remove blinker dependece, no signals working in building progress.
